# Inter Polation
means we can reuse any variable declared inside the .ts using `{{}}` by passing the variable name inside double curly bracket

## Angular cli
Angular command line interface is a tool thats help developer to create and build and deploy angular app

## Angular cli command
